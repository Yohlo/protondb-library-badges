{
  "name": "protondb-library",
  "version": "2.0.0",
  "description": "ProtonDB compatibility tiers in the desktop Steam library: inline game-page stat, sidebar indicators, and a tier section in the Library Filters panel.",
  "type": "module",
  "main": "./frontend/index.tsx",
  "license": "MIT",
  "scripts": {
    "dev": "millennium-ttc --build dev",
    "build": "millennium-ttc --build prod"
  },
  "dependencies": {
    "@steambrew/client": "^5.5.3",
    "@steambrew/ttc": "^2.7.3"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "typescript": "^5.0.0"
  }
}
